@JArchParametersViewShow(menus = {
        @Menu(labelMenu = "label.", tabs = {
                @Tab(fieldSets = {
                        @FieldSet(parameters = {

                        })
                })

        }),
        @Menu(labelMenu = "label.", tabs = {
                @Tab(fieldSets = {
                        @FieldSet(parameters = {
                        })
                })

        }),
})

//@JArchParametersViewConfig(viewsConfig = {
//        @ViewConfig(
//                parameter = .class,
//                filterSelection = .class
//        ),
//        @ViewConfig(
//                parameter = .class,
//                filterSelection = .class
//        )
//})

package reddy.flip2.web.parameter;

//import br.com.jarch.faces.parameter.JArchParametersViewConfig;
import br.com.jarch.faces.parameter.JArchParametersViewShow;

//import static br.com.jarch.faces.parameter.JArchParametersViewConfig.*;
import static br.com.jarch.faces.parameter.JArchParametersViewShow.*;
